import 'package:flutter/material.dart';

/// Страница истории показаний счетчиков
class CountersHistory extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('История счетчиков'),
    );
  }
}
