import 'package:flutter/material.dart';

/// Страница подачи показаний счетчиков
class CountersSupply extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Подача показаний'),
    );
  }
}
