import 'package:flutter/material.dart';

/// Страница создания заявок
class RequestsCreate extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text('Создание заявки'),
    );
  }
}
